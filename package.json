{
  "name": "taeyang-city-branch-intranet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/validate.mjs",
    "dev": "netlify dev",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.2"
  },
  "devDependencies": {
    "netlify-cli": "^23.0.0"
  }
}
